name: Publish to NPM Registry

on:
  push:
    branches:
      - main

permissions:
  id-token: write

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/utility' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@d9962435db3e6cef4df7b2b745e609553760ea24 # v2.8.7
