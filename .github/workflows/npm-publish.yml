name: Publish to NPM Registry

on:
  push:
    branches:
      - main

permissions:
  id-token: write

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/utility' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@701bb82ccd8b7b289794c3b2f8c056ae0b31e359 # v3.0.2
