name: Publish to NPM Registry

on:
  push:
    branches:
      - main

permissions:
  id-token: write

jobs:
  publish:
    if: ${{ github.repository == 'AlexMan123456/utility' }}
    uses: AlexMan123456/github-actions/.github/workflows/npm-registry-publish.yml@c817738b1014c70c514f5b7008122fb06191e57e # v3.0.5
