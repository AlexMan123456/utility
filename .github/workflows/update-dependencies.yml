name: Update Dependencies
on:
  workflow_dispatch:
  schedule:
  - cron: 0 21 * * 5

permissions:
  contents: write
  pull-requests: write

jobs:
  update-dependencies:
    if: ${{ github.repository == 'AlexMan123456/utility' }}
    uses: AlexMan123456/github-actions/.github/workflows/update-dependencies.yml@c9e8f4a524509cd563f994896a677fdb8ce794dc # v2.8.6
    secrets:
      PAT_GITHUB: ${{ secrets.PAT_GITHUB }}
